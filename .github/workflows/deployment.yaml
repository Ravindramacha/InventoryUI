name: Deploy Vite React App to Azure Static Web Apps

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write
  actions: read

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js 22
        uses: actions/setup-node@v3
        with:
          node-version: 20.19.0 # ✅ Use latest compatible version

      - name: Clean install dependencies
        run: |
          npm install
        working-directory: InventoryApp  # Change to your app dir if needed

      - name: Build Vite app
        run: npm run build
        working-directory: InventoryApp

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "InventoryApp"
          output_location: "InventoryApp/dist"
