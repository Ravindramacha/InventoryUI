{
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": [
      "/api/*"
    ]
  },
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://inventory-webapi-dev.azurewebsites.net/api/:*",
      "allowedRoles": ["anonymous"]
    }
  ]
}